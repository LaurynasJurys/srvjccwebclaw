# 2026-04-19

- Repositioned `staging.jurys.lt` around AI deployment as the main business, with Azure and disaster recovery as supporting services.
- Simplified the services page for mobile and sales use, and added a new `training.html` page for text trainings and video trainings.
- Enabled the OpenClaw Discord channel using bot credentials stored in Vault under `secret/Discord`.
- Confirmed Vault root credentials live under `/root/.vault` and should only be used when explicitly authorized.
- Recorded/confirmed the standing rule that secrets must never be shared in chat and should only be stored/retrieved through Vault.
- Discord is now intended to become the main communication surface instead of WhatsApp.
- Discord access is currently configured with owner allowlisting and the `general` channel on guild `1485021229638226125` enabled for slash command use.
- The current Discord Vault secret shape is imperfect because `ChannelId` was actually used as a guild id; this should be cleaned up later into distinct `GuildId` and `ChannelId` fields.
- Laurynas wants a path toward talking to OpenClaw by voice, likely starting from Discord before adding a more advanced voice flow later.
