# 2026-04-17

- Created initial long-term memory file (`MEMORY.md`).
- Disabled heartbeat for now.
- OpenClaw web UI is exposed via Tailscale.
- Fixed credentials directory permissions.
- Moved gateway password secret into `.env` and config now references `OPENCLAW_GATEWAY_PASSWORD`.
- Memory search enabled, but semantic provider setup deferred for later.
# 2026-04-17

- Created initial long-term memory file (`MEMORY.md`).
- Disabled heartbeat for now.
- OpenClaw web UI is exposed via Tailscale.
- Fixed credentials directory permissions.
- Moved gateway password secret into `.env` and config now references `OPENCLAW_GATEWAY_PASSWORD`.
- Memory search enabled, but semantic provider setup deferred for later.
- Hardened SSH by confirming `PasswordAuthentication no` and changing `PermitRootLogin` to `no`.
- Bootstrapped k3s + Argo CD on the host, fixed Argo repo credential/bootstrap issues, and verified ingress/TLS for staging, Keycloak, and Wiki.js.
- Chose manual-first Keycloak bootstrap going forward and removed bootstrap admin credentials from both repo manifests and the live Keycloak Kubernetes secret.
- Set up SOPS + age for GitOps secrets, including Argo-side decryption support, and moved Keycloak/Wiki.js Kubernetes secrets to encrypted GitOps-managed config.
- Deployed Vault on the host outside k3s, bound privately on loopback + Tailscale only, initialized/unsealed it, and enabled KV v2.
- Established Vault AppRole-based automation access and granted scoped read access to the Keycloak secret path.
- Created Keycloak realm `jcc` and configured a `WikiApp` OIDC client; realm hardening enabled email verification, reset password, brute-force protection, disabled registration, and disabled direct access grants for the Wiki client.
- Began replacing staging.jurys.lt Traefik basic-auth with Keycloak-backed oauth2-proxy via GitOps; repo commit `d57fef9` exists but rollout was not yet fully completed or verified because the Keycloak client secret needed one cleanup/fix.
